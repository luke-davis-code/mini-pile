import PaintingListController from './PaintingListController'
import Settings from './Settings'

const Controllers = {
    PaintingListController: Object.assign(PaintingListController, PaintingListController),
    Settings: Object.assign(Settings, Settings),
}

export default Controllers